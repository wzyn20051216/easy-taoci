# 隐私与安全

本项目处理的材料通常包含姓名、教育经历、成绩、联系方式、简历、成绩单和浏览器登录状态。默认按敏感个人信息处理。

## 不进入 Git 的内容

- `private/`：学生档案、邮件模板、附件、追踪表和浏览器配置。
- `workspace/`：真实候选名单、排序结果、草稿、截图和运行状态。
- 任何密码、验证码、Cookie、邮箱会话参数、访问 token、身份证号或手机号。

## 日志最小化

浏览器运行状态默认只记录草稿任务 ID、结果、时间和非敏感错误类型。任务 ID 由候选与主题生成的哈希构成，无法直接还原正文。调试截图需要显式开启，并只放在 `workspace/`。

## 发布前检查

1. 运行隐私扫描。
2. 检查 `git status --short`，确认没有 `private/`、`workspace/` 或浏览器配置。
3. 检查提交历史，而不仅是当前文件。
4. 若敏感信息曾经提交，不要只在新提交中删除；应先停止发布并清理整个 Git 历史，再轮换可能泄露的凭据。

```powershell
python -m easy_taoci.cli privacy-scan --path . --deny-file private/privacy-deny.txt
git grep -n -I -E "sid=|cookie|token|password|验证码"
```

`private/privacy-deny.txt` 每行填写一个当前用户专属敏感片段。建议包含姓名、邮箱用户名、手机号、旧工作区目录名和不应公开的项目代号。扫描器也会检查被跟踪文件的路径名。

扫描器只是一道防线，不能替代人工审查。
