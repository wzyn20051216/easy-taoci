# 贡献指南

欢迎改进高校官网检索、内容质量、浏览器适配和追踪表同步。

## 开发原则

- 示例只使用虚构学校、姓名和 `example.*` 邮箱域名。
- 不提交真实简历、成绩单、导师清单、邮件正文、截图或浏览器配置。
- 新增浏览器适配时默认只预检；任何写操作都需要显式开关。
- 不为邮件发送提供自动化实现。保存草稿与发送必须保持清晰边界。
- 新增评分特征时给出可解释理由，避免把启发式分数包装成录取概率。
- 官网信息可能变化，时效性字段必须保留来源和核验日期。

## 提交前

```powershell
python -m unittest discover -s tests -v
python -m taoci_email_tailoring.cli privacy-scan --path .
python -m compileall -q taoci_email_tailoring tests
```

新增功能应带离线测试。浏览器测试若无法在 CI 中运行，至少测试输入预检、状态恢复、选择器辅助逻辑和日志最小化。

## 扩展邮箱适配器

适配器建议复用 `drafts.jsonl` 和 `state.jsonl` 数据合同，并满足：

1. 默认 dry-run。
2. 显式执行参数。
3. 稳定任务 ID 与断点续跑。
4. 每封失败即停。
5. 成功前回读关键字段和附件状态。
6. 日志不记录正文、收件人和本地附件路径。

## 扩展导师数据源

优先贡献“如何枚举和核验”的通用适配，不要提交抓取到的真实个人数据。解析器输出应映射到 `references/data-contracts.md`，并保留原始官方 URL、核验日期和不确定性。
